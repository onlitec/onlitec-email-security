# Actions configuration

reject = 15;
quarantine = 10;
add_header = 6;
greylist = 4;

# Custom actions mapped to our logic
# Note: composite_score.lua may override these dynamically using set_metric_action
