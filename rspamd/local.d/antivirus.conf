# Rspamd Antivirus Configuration - Simplified
clamav {
  type = "clamav";
  servers = "onlitec_clamav:3310";
  symbol = "CLAM_VIRUS";
  
  # Scan settings
  scan_mime_parts = true;
  scan_text_mime = false;
  scan_image_mime = false;
  
  # Limits
  max_size = 52428800; # 50MB
  timeout = 15.0;
  
  # Handling
  action = "reject";
}
